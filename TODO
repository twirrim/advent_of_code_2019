Change immediate / position mode to use an enum

Can I modify OC::Input and OC::Output so that it would be possible to write tests for them?
Update OC::Add and OC::Mul tests to account for immediate and position mode